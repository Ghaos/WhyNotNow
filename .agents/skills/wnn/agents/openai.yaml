interface:
  display_name: "Why Not Now?"
  short_description: "Explore a deferred task through contextual dialogue."
  default_prompt: "Use $wnn to capture a task, explore its context, and optionally investigate a concrete blocker."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "why-not-now"
      description: "Shows structured WhyNotNow forms and handles private conversation persistence."
