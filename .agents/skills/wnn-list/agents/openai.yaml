interface:
  display_name: "Why Not Now List"
  short_description: "List saved WhyNotNow conversations."
  default_prompt: "Use $wnn-list to show my saved WhyNotNow conversations."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "why-not-now"
      description: "Lists compact summaries of private WhyNotNow conversations."
