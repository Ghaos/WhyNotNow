{
  "name": "why-not-now",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Codex skill that captures tasks, reasons to do them, and reasons not to do them now.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check .agents/skills/why-not-now/scripts/store.mjs && node --check .agents/skills/why-not-now/scripts/whynotnow.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
